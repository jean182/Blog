<%= form_for(comment) do |f| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, 'Usuario' %>
    <%= f.text_field :user_id, placeholder: 'Ingrese el Usuario', class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :article_id, 'Artículo' %>
    <%= f.text_field :article_id, placeholder: 'Ingrese el Artículo', class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :body, 'Cuerpo' %>
    <%= f.text_area :body, placeholder: 'Ingrese el comentario', class: 'form-control', rows: '5' %>
  </div>

  <div class="form-group">
    <%= f.submit 'Enviar', class: 'btn btn-primary' %>
  </div>
<% end %>
